{% extends 'base.html.twig' %}

{% block body %}
{#        <div class="card-columns">#}
{#            {% for photoArray in photos %}#}
{#                {% for photo in photoArray %}#}
{#                    <div class="card m-3">#}
{#                        <a class="aphoto" href="{{ path('google_photos_single_media', {'mediaId': photo.id, "albumId": album}) }}"><img class="card-img-top" src="{{ photo.url }}">#}
{#                            <div class="card-body">#}
{#                                <h5 class="card-title">{{ photo.text }}</h5>#}
{#                                <p class="card-text"><small class="text-muted">Created: {{ photo.createdtime }}</small></p>#}
{#                            </div>#}
{#                        </a>#}
{#                    </div>#}
{#                {% endfor %}#}
{#            {% endfor %}#}
{#        </div>#}
    <div class="row">
    {% for photoArray in photos %}
        <div class="column">
            {% for photo in photoArray %}
                <div class="card my-2">
                    <a class="aphoto" href="{{ path('google_photos_single_media', {'mediaId': photo.id, "albumId": album}) }}"><img class="card-img-top" src="{{ photo.url }}">
                        <div class="card-body">
                            <h5 class="card-title">{{ photo.text }}</h5>
                            <p class="card-text"><small class="text-muted">Created: {{ photo.createdtime }}</small></p>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    {% endfor %}

    </div>
{% endblock %}